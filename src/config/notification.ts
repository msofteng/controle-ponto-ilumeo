import { NotificationData } from '@mantine/notifications';

const options: Partial<NotificationData> = {
    color: 'var(--bg-color-primary)',
    radius: 'md',
    bg: 'rgba(var(--bg-color-default-rgb), 0.9)',
};

export default options;
